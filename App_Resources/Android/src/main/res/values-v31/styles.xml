<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Android 12+ Splash Screen API -->
    <style name="LaunchScreenThemeBase" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="toolbarStyle">@style/NativeScriptToolbarStyle</item>

        <item name="colorPrimary">@color/ns_primary</item>
        <item name="colorPrimaryDark">@color/ns_primaryDark</item>
        <item name="colorAccent">@color/ns_accent</item>

        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowTranslucentStatus">true</item>

        <!-- SplashScreen API (Android 12+) -->
        <item name="android:windowSplashScreenBackground">#F5C542</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_app_logo</item>
        <item name="android:windowSplashScreenIconBackgroundColor">#20FFFFFF</item>
    </style>

    <style name="LaunchScreenTheme" parent="LaunchScreenThemeBase">
    </style>

    <!-- Application theme inherits from v29 -->
    <style name="AppTheme" parent="AppThemeBase29">
    </style>

</resources>
